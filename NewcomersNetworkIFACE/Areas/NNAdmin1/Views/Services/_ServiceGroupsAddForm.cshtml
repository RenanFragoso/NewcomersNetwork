@model NewcomersNetworkAPI.Models.ServicesSchedule
@{
    var newSchedule = (Model.Id == "NULLID");
}
<div class="row-fluid">
    <div class="span12">

        <div class="widget-box">
            <div class="widget-title">
                <span class="icon"> <i class="icon-user"></i> </span>
                <h5>@(newSchedule ? "New " : "" )Schedule @(newSchedule ? "" : "Update" )</h5>
            </div>
            <div class="widget-content nopadding">

                <form class="form-horizontal" id="ScheduleAddForm">

                    @Html.AntiForgeryToken()
                    @Html.HiddenFor(model => model.Id)

                    <div class="control-group">
                        @Html.LabelFor(model => model.Name, new { @class = "control-label" })
                        <div class="controls">
                            @Html.TextBoxFor(model => model.Name, new { @class = "span11", placeholder = "Schedule Name" })
                        </div>
                    </div>

                    <div class="control-group">
                        @Html.LabelFor(model => model.Description, new { @class = "control-label" })
                        <div class="controls">
                            @Html.TextBoxFor(model => model.Description, new { @class = "span11", placeholder = "Schedule Description" })
                        </div>
                    </div>

                    <div class="control-group">
                        @Html.LabelFor(model => model.StartDate, new { @class = "control-label" })
                        <div class="controls">
                            <div data-date="(@DateTime.Now.Date.ToString("dd/MM/yyyy"))" class="input-append date datepicker">
                                <input id="dateStrt" name="StartDate" type="text" class="span11" data-date='@(Model.StartDate.Date.ToString("dd/MM/yyyy").Replace("-","/"))' value='@(Model.StartDate.Date.ToString("dd/MM/yyyy").Replace("-","/"))'/>
                                <span class="add-on"><i class="icon-th"></i></span>
                            </div>
                        </div>
                    </div>

                    <div class="control-group">
                        @Html.LabelFor(model => model.EndDate, new { @class = "control-label" })
                        <div class="controls">
                            <div data-date="(@DateTime.Now.Date.ToString("dd/MM/yyyy"))" class="input-append date datepicker">
                                <input id="dateEnd" name="EndDate" type="text" class="span11" data-date='@(Model.EndDate.Date.ToString("dd/MM/yyyy").Replace("-","/"))' value='@(Model.EndDate.Date.ToString("dd/MM/yyyy").Replace("-","/"))' />
                                <span class="add-on"><i class="icon-th"></i></span>
                            </div>
                        </div>
                    </div>

                    <div class="control-group">
                        @Html.LabelFor(model => model.Responsible, new { @class = "control-label" })
                        <div class="controls">
                            @Html.TextBoxFor(model => model.Responsible, new { @class = "span11", placeholder = "Responsible" })
                        </div>
                    </div>

                    <div class="control-group">
                        @Html.LabelFor(model => model.Enable, new { @class = "control-label" })
                        <div class="controls">
                            @Html.CheckBoxFor(model => model.Enable, new { @class = "control-label" } )
                        </div>
                    </div>

                    <div class="control-group" id="FormErrors">
                        <div class="alert alert-error alert-block" id="ErrorsSummary" style="visibility: hidden">
                        </div>
                    </div>

                    <div class="form-actions">
                        <button type="submit" class="btn btn-success">Save</button>
                    </div>

                  </form>


                </div>
            </div>
        </div>
    </div>

<style>
    .datepicker.dropdown-menu {
        z-index: 100000 !important;
    }
</style>